// Replaced at container start from the environment - see client/docker-entrypoint.sh.
// Empty here so `yarn dev` and non-container builds fall back to VITE_* values.
window.__SKYOFFICE_CONFIG__ = {}
